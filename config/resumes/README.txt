Drop your resume PDFs here:

  resume_python.pdf     - Python / Django / Flask / FastAPI roles
  resume_java.pdf       - Java / Spring / Hibernate roles
  resume_fullstack.pdf  - React / Node / Angular / full-stack roles

The app picks one automatically based on keywords in each job description
(see RESUME_KEYWORD_MAP in config/config.py). If a file is missing, cold mails
are still sent/queued without an attachment and a warning is logged.
